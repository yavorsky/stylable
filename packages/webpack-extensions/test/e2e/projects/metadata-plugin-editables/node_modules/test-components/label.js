import { classes } from './label.st.css';
const render = (_text = 'This is a Label') => {
    const lbl = document.createElement('label');
    lbl.textContent = _text;
    lbl.classList.add(classes.root);
    return lbl;
};
export { classes, render };
